extends dashboard.pug

block dashboard-content
    <div id="dashboard-card" class="card">
        <div id="dashboard-card-content" class="card-content">
            <div class="card-title center">Ejercicios</div>
            <div class="row">
            each exercise in exercises
                include ../partials/exercise
                +exercise(exercise)
            </div>
        </div>
    </div>